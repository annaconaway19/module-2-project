<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <nav class="navbar-navbar default">

      <div class="container fluid">

      <div class="navbar-header">
         <%= link_to 'Cocktail Finder', posts_path(session[:user_id]) %>
        </div>

            <ul class="navbar-nav">
            <li class="active">
                <%= link_to 'Home', posts_path(session[:user_id]) %><span class="sr-only">(current)</span>
            </li>
            <li><%= link_to 'My Profile', account_path(session[:user_id]) %></li>
            <li><%= link_to 'My Drinks', user_path(session[:user_id]) %></li>
            <li><%= link_to 'Cocktail Catalogue', cocktails_path(session[:user_id]) %></li>
            <li><%= link_to 'Create A Cocktail', new_post_path %></li>
          </ul>

          </div>
      </nav>
  </body>
</html>
