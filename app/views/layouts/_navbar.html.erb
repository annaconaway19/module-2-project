<nav class="navbar-expand-lg navbar-light bg-light">

  <a class="navbar-brand" <%= link_to 'Cocktail Finder', posts_path(session[:user_id]) %></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <%= link_to 'Home', posts_path(session[:user_id]) %><span class="sr-only">(current)</span>
        </li>
        <li class="nav-item">
          <%= link_to 'My Profile', account_path(session[:user_id]) %>
        </li>
        <li class="nav-item">
          <%= link_to 'My Drinks', user_path(session[:user_id]) %>
        </li>
        <li class="nav-item">
          <%= link_to 'Cocktail Catalogue', cocktails_path(session[:user_id]) %>
        </li>
      </ul>
    </div>
  </nav>
