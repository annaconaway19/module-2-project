<% if logged_in? && current_user.like?(@post) %>
  <%= link_to "Unlike", likes_path(@post), method: :delete, remote: true %>
<% else %>
  <%= link_to "Like", likes_path(@post), method: :post, remote: true %>
<% end %>
